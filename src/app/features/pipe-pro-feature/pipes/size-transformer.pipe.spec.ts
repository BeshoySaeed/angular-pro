import { SizeTransformerPipe } from './size-transformer.pipe';

describe('SizeTransformerPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeTransformerPipe();
    expect(pipe).toBeTruthy();
  });
});
